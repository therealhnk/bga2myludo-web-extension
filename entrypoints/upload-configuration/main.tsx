import React from "react";
import ReactDOM from "react-dom/client";
import UploadConfigurationPage from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <UploadConfigurationPage />
    </React.StrictMode>
);
