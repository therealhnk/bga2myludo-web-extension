import React from "react";
import ReactDOM from "react-dom/client";
import BoardingPage from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <BoardingPage />
    </React.StrictMode>
);
